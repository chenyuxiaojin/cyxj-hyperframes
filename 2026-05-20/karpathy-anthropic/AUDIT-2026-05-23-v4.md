# AUDIT v4 2026-05-23 — 校准：以 npm bundle 内 SKILL.md + references 为生产标准

## 校准决定

用户决定（2026-05-23）：**HeyGen 是 HyperFrames 最大的生产方，他们随 npm 包发的 curated skill (`dist/skills/hyperframes/SKILL.md` + `references/*.md`) 就是生产标准。** 公开网站文档是简化版的开发者入门，curated skill 才是 HeyGen 内部经验沉淀，要按它做。

→ **v2 的严标准清单是正确的工作清单。v3 把它降级是过度让步，作废。**

## 权威源（v4 最终版）

| 层级 | 来源 | 权威性 |
|---|---|---|
| **L1 生产标准** | npm `hyperframes@0.6.34` 包内 `dist/skills/hyperframes/SKILL.md` + `references/*.md` | **HeyGen 内部生产标准 codify**，**最权威** |
| **L1 GSAP** | gsap.com/docs/v3 | GSAP 权威 |
| **L2 简化版开发者文档** | hyperframes.heygen.com / mintlify 镜像 | 入门文档，规则不全 |
| **L2 lint 实际行为** | `dist/cli.js` | 行为权威 |
| **不参考** | 本仓库 `docs/HARD_CONSTRAINTS.md` / `MY_VISUAL_DNA.md` / `CLAUDE.md` / `MY_MOTION_NOTES.md` | — |

## 工作清单 = v2 的 P0/P1/P2/P3 列表

详见 `AUDIT-2026-05-23-v2.md` §10 表。再复述一次：

### P0 必修

1. **seg06 t=732-818s `text_box_overflow` ERROR**
   - 依据：`SKILL.md:391-400`（inspect 输出必须 0 issue 或标 intentional）
   - 文件：`compositions/seg06-merge.html` 50-60s 段
   - 估时：15-30 min

2. **9 段 sec 切换 hard cut → crossfade**
   - 依据：`references/transitions.md:5-12` 4 条 Non-Negotiable（"Every composition uses transitions. No exceptions."）
   - 文件：ch0-cold-open + seg01-08 共 9 文件 ~90 处
   - 估时：2-4 小时
   - 最低成本方案：换 `transitions.md:70-79` 给的 `smooth` preset（0.4s `power2.inOut`）

3. **<14px 微标签全量提到 ≥18px**
   - 依据：`references/video-composition.md:35-46`（labels video 18-24px / "<24px 必须 justify"）
   - 文件：5+ 处确切 + 全片再扫一遍
   - 估时：30 min

### P1

4. **Noto Sans SC 本地化 .woff2 + @font-face**
   - 依据：`SKILL.md:363-364` "Custom fonts: ... the user must provide `.woff2` files"
   - 文件：`index.html` + `assets/fonts/`
   - 估时：30 min（下载 Noto Sans SC Variable woff2 ~280KB）

5. **seg01:1069 第一动画 `0.0` → `0.15-0.2`**
   - 依据：`SKILL.md:352` + `motion-principles.md:12` "Offset first animation 0.1-0.3s (not t=0)"
   - 估时：30 秒

6. **seg06:1412 `tl.to(.a-confluence-text, autoAlpha:0)` 排查是否违反 transitions Rule 3**
   - 依据：`transitions.md:11` "Exit animations are BANNED except on the final scene"
   - 排查 5 min（如果是 sec-a 内 element 被提前 fade out，需要改）

### P2

7. **14-19px label / data chip 提到 18-24px**（视频生产标准 video-composition.md）
8. **数字列加 `font-variant-numeric: tabular-nums`**（SKILL.md:357）
9. **seg04:1299 `gsap.set` → `tl.set(..., 0)` 风格统一**（跨段一致）

### P3

10. **102 处 `<img>` 加 `crossorigin="anonymous"`**（SKILL.md:365）
11. **marker-sweep 故意越界加 `data-layout-allow-overflow="true"`**（SKILL.md:402）

### Silence（lint 误报 / 跟 SKILL.md 示例本身冲突）

- 1247 条 `composition_self_attribute_selector`（SKILL.md:177 示例本身就用 `[data-composition-id="X"]`）
- 11 条 `composition_file_too_large`（SKILL.md 全文无 300 行硬阈值）
- 11 条 `duplicate_media_discovery_risk`（SKILL.md 全文无此规则）

---

## 三份审计文件的角色

| 文件 | 作用 |
|---|---|
| `AUDIT-2026-05-23.md` (v1) | 初版，混引了仓库本地 HARD_CONSTRAINTS。**有用错的引文，但 P0 真问题（seg06 + 字体）正确**。可保留参考。 |
| `AUDIT-2026-05-23-v2.md` (v2) | 切到 npm SKILL.md + references。**P0-P3 清单正确**（v4 沿用）。v2 的"上游官方"用词不够准——准确说法是"HeyGen 生产标准"。 |
| `AUDIT-2026-05-23-v3.md` (v3) | 切到 heygen.com 简化版官网。**降级清单过度让步，作废**。 |
| `AUDIT-2026-05-23-v4.md`（本文件）| 校准：v2 是工作清单。 |

---

## 下一步建议

按 P0 顺序动：
1. **seg06 t=732-818s 排查**（最快 15 min，可能是真 bug 也可能是 GSAP x 平移的预期）→ 跑 `npx hyperframes inspect --at 732,750,780,800,818 --json` 看具体哪个 element / 哪个 tween
2. **9 段 sec 硬切 → smooth crossfade**（最大工作量，但视觉提升最直观）→ 用 sed/批量改 + 全片重 preview scrub
3. **<14px 微标签批量上调**（30 min sed 找替换）

要不要现在动 P0-1（seg06 排查）？这个最快出结论。
